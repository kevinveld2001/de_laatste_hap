import 'package:flutter/material.dart';

class Verlanglijst extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
          child:Text("verlanglijst"),
        );
  }
}